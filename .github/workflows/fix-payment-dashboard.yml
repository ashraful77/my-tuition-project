# temporary workflow disabled; verify-refactor performs the approved patch
name: Fix payment dashboard and filters
on:
  push:
    branches: [refactor/student-card-v47]
permissions:
  contents: write
jobs:
  fix:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled; payment dashboard fix is handled by the verifier workflow."
